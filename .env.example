# Placeholder — variables reales se agregan a medida que se implementen
# backend, base de datos, auth y realtime (ver 04-tech-stack.md, 09-security-spec.md,
# 14-deployment-spec.md).
#
# Nunca commitear un .env real con valores verdaderos.

NODE_ENV=development
DATABASE_NAME=trading_analytics_dev
DATABASE_USER=trading_user
DATABASE_PASSWORD=change_me_in_local_env
DATABASE_PORT=5432
DATABASE_URL="postgresql://trading_user:change_me_in_local_env@localhost:5432/trading_analytics_dev?schema=public"